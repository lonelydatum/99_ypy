{
  "name": "banner-build",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node scripts/dev.mjs",
    "dev:banner": "NODE_ENV=development node scripts/dev.mjs --banner",
    "build": "NODE_ENV=production node scripts/build.mjs",
    "release": "NODE_ENV=production npm run build && npm run zip && npm run serve",
    "serve": "node scripts/serve.mjs",
    "tinyimages": "node scripts/optimize-images.cjs",
    "zip": "node scripts/zip-banners.mjs"
  },
  "devDependencies": {
    "archiver": "^7.0.1",
    "browser-sync": "^3.0.4",
    "chokidar": "^4.0.3",
    "ejs": "^3.1.10",
    "esbuild": "^0.25.0",
    "fast-glob": "^3.3.3",
    "fs-extra": "^11.2.0",
    "sass": "^1.77.8"
  },
  "dependencies": {
    "optipng-bin": "^9.0.0",
    "pngquant-bin": "^9.0.0",
    "sharp": "^0.34.5"
  }
}
